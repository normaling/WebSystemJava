#数据库配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mangersystem
    username: root
    password: 123456
  servlet:
    #配置文件上传大小限制
    multipart:
      max-request-size: 10MB
      max-file-size: 100MB
#mybatis配置
#log-impl配置mybatis日志，指定输出到控制台
#开启mybatis的驼峰命名开关，a_column-->aCloumn
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

#GitHub图床配置(已弃用）
github:
  bucket:
    # 配置仓库所属的用户名（如果是自己创建的，就是自己的用户名）
    user: "normaling"
    # 配置仓库名称
    repository: "image-bucket"
    # 配置自己的acccessToken
    access-token: "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"
    url: "https://cdn.jsdelivr.net/gh/${github.bucket.user}/${github.bucket.repository}/"
    api: "https://api.github.com/repos/${github.bucket.user}/${github.bucket.repository}/contents/"



